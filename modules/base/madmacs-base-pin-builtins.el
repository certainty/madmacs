;; -*- lexical-binding: t; -*-

;; inform straight about builtin packages

(use-package project
  :straight (:type built-in))

(use-package whichkey
  :straight (:type built-in))

(provide 'madmacs-base-pin-builtins)
